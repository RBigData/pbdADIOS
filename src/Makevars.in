#################################################################

PKG_CPPFLAGS = @MPI_CPPFLAGS@ @ADIOS_CPPFLAGS@
PKG_CFLAGS = -w
PKG_LIBS = @MPI_LDFLAGS@ @ADIOS_LDFLAGS@


OBJECTS = R_adios_read.c


### Start making here.
all: $(SHLIB)

$(SHLIB): $(OBJECTS)


clean:
	@rm -rf *.o *.mod *.d *.rc *.so *.dylib *.dll *.a *.lib \
		Makedeps Makevars $(USER_CONF) $(SHLIB) $(OBJECTS) 
